postgresql-test = {
  url = "jdbc:postgresql://localhost/test_api"
  driver = org.postgresql.Driver
  properties = {
    user = "postgres"
    password = ""
  }
  connectionTestQuery = "select version();"
}

akka {
  loglevel = "INFO"
}
