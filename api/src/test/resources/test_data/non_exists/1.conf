description: "non-existing URL"

urls_common: [/, /ap, /api/a] // helper field

requests = [{
  method: GET
  urls: ${urls_common}
}, {
  method: POST
  urls: ${urls_common}
}]

response {
  status: 404
}
