kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: gnresolver-pvc
  namespace: gn
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 300Gi
  selector:
    matchLabels:
      app: gnresolver
      tier: backend
      type: master
      node: "1"
