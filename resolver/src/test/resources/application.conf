postgresql-test = {
  url = "jdbc:postgresql://localhost/circle_test"
  driver = org.postgresql.Driver
  properties = {
    user = "ubuntu"
    password = ""
  }
  connectionTestQuery = "select version();"
}
