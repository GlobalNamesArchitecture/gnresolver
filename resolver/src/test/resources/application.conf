postgresql-test = {
  url = "jdbc:postgresql://localhost/test_resolver"
  driver = org.postgresql.Driver
  properties = {
    user = "postgres"
    password = ""
  }
  connectionTestQuery = "select version();"
}
